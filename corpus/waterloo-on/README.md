# waterloo-on

Dual-tier lower-tier pack. Build:

```bash
python scripts/build_lower_tier_pack.py waterloo-on
python scripts/validate_pack.py waterloo-on
```

Region area key: `urban` (shared with Kitchener / Cambridge).
