# cambridge-on

Dual-tier lower-tier pack (GAP-tolerant departments). Build:

```bash
python scripts/build_lower_tier_pack.py cambridge-on
python scripts/validate_pack.py cambridge-on
```

Region area key: `urban`.
